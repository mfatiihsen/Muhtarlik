@model List<muhtarlik.Models.Dilekce>

@{
  Layout = "_AdminLayout";
  ViewData["Title"] = "Dilekçeler";
}


<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
<div class="container py-4">
  <div class="d-flex justify-content-between align-items-center mb-3">
    <h3 class="mb-0">📄 Dilekçeler</h3>
  </div>

  <table class="table table-bordered table-hover align-middle">
    <thead class="table-light">
      <tr>
        <th>Ad Soyad</th>
        <th>TC</th>
        <th>Başlık</th>
        <th>İçerik</th>
        <th>Telefon</th>
        <th>Tarih</th>
        <th style="width: 130px;">İşlemler</th>
      </tr>
    </thead>
    <tbody>
      @foreach (var dilekce in Model)
      {
        <tr>
          <td>@dilekce.AdSoyad</td>
          <td>@dilekce.TCKimlikNo</td>
          <td>@dilekce.Baslik</td>
          <td>@dilekce.Icerik</td>
          <td>@dilekce.Telefon</td>
          <td>@dilekce.Tarih.ToString("dd.MM.yyyy")</td>
          <td>
            <form asp-action="DilekceSil" asp-controller="Admin" method="post"
              onsubmit="return confirm('Bu dilekçeyi silmek istediğinize emin misiniz?');">
              <input type="hidden" name="id" value="@dilekce.Id">
              <button class="btn btn-sm btn-danger">Sil</button>
            </form>
          </td>
        </tr>
      }

    </tbody>
  </table>
</div>

<!-- Dilekçe Ekle Modal -->
<div class="modal fade" id="dilekceModal" tabindex="-1" aria-labelledby="dilekceModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <form>
        <div class="modal-header">
          <h5 class="modal-title" id="dilekceModalLabel">Yeni Dilekçe Ekle</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Kapat"></button>
        </div>
        <div class="modal-body">
          <div class="mb-3">
            <label class="form-label">Ad Soyad</label>
            <input type="text" class="form-control" placeholder="Örn: Mehmet Kaya">
          </div>
          <div class="mb-3">
            <label class="form-label">Konu</label>
            <input type="text" class="form-control" placeholder="Dilekçenin konusu">
          </div>
          <div class="mb-3">
            <label class="form-label">Açıklama</label>
            <textarea class="form-control" rows="4" placeholder="Detaylı açıklama..."></textarea>
          </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Vazgeç</button>
          <button type="submit" class="btn btn-primary">Kaydet</button>
        </div>
      </form>
    </div>
  </div>
</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
